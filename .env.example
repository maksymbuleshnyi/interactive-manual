# Copy to .env and fill in. .env is gitignored. Never commit real keys.
# Stage 0-1 run fully on mock adapters and need none of these.

# Adapter selection. Resolved via models/registry.py (added in Stage 1).
II_LLM=mock
II_EDITOR=mock
II_CRITIC=mock

# Stage 2 - LLM provider (vendor TBD; record the choice in docs/DECISIONS.md).
# II_LLM_API_KEY=

# Stage 3 - image-edit provider (vendor TBD).
# II_IMAGE_API_KEY=
